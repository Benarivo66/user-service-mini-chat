# user-service-mini-chat
We can run the app with `npm run start` or `npm run server`

We can spin up the docker container with `docker compose up`

Manual test sample is found in the screenshots folder